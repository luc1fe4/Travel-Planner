apiVersion: v1
kind: Service
metadata:
  name: travel-planner-service
  labels:
    app: travel-planner
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: travel-planner
